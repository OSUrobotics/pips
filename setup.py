from setuptools import setup, find_packages

setup(
    name='pips',
    version='1.0',
    packages=find_packages(),
    url='',
    author='',
    author_email='',
    description='PIPs',
    install_requires=[
    ],
)